{
  "name": "finish",
  "description": "[Git flow]: Finish and merge branch",
  "steps": {
    "merge": {
      "implementation-check" : false
    },
    "validate" : {
      "excludes" : ["feature", "hotfix", "release", "merger"],
      "type" : "flow"
    },
    "setVersion": {
      "excludes" : ["feature", "hotfix", "release", "merger"]
    },
    "finish": {
      "implementation-check" : false
    },
    "publish": {
      "type" : "flow",
      "params" : {
        "tag": true
      },
      "inputs" : {
        "deletedBranch" : {"finish": "deletedBranch"}
      }
    }
  }
}
